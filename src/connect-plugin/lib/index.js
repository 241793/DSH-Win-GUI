/** Host-side entry for dsh-connect-center: no host behavior.
 *  Channel management is done by the Electron main process over the IPC bridge. */
const name = "connect-center-host";
function apply(_ctx) {}
export { apply, name };
